const Header = () => {
  return (
    <header>
      Header signature leading to home about projects (list of projects with
      more description and details) itbible Contact {'->'} shall be an
      integration with calendly
    </header>
  );
};

export default Header;
